export enum AppState {
  READY = "ready",
  PROCESSING = "processing",
  FINISHED = "finished",
}
export enum ImageFileFormats {
  JPEG = "jpg",
  PNG = "png",
}
export enum Container {
  NONE = "",
  SVG = "svg",
}
